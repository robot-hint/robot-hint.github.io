---
import { Picture as AstroPicture } from "astro:assets";

interface Props {
  src: ImageMetadata;
  alt: string;
  priority?: boolean;
  invertInDarkMode?: boolean;
}

const { src, alt, priority = false, invertInDarkMode = false } = Astro.props;
---

<AstroPicture
  class={`mx-auto max-h-[calc(100svh-3rem)] w-min max-w-full ${invertInDarkMode ? "dark:invert" : ""}`}
  src={src}
  alt={alt}
  layout="constrained"
  formats={["avif"]}
  priority={priority}
  fit="contain"
/>
